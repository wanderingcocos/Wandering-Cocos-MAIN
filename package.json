{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:prod": "npx --yes pnpm@9 install && BASE_PATH=/ npx --yes pnpm@9 --filter @workspace/wandering-cocos run build && npx --yes pnpm@9 --filter @workspace/api-server run build",
    "prestart": "npm run build:prod",
    "start": "node artifacts/api-server/dist/index.cjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "@img/sharp-linux-x64",
      "@img/sharp-linux-arm64",
      "@img/sharp-darwin-x64",
      "@img/sharp-darwin-arm64"
    ],
    "allowedBuilds": ["esbuild", "sharp"]
  }
}
